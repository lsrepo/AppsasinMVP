Usage 1: Assign a target
When two iPhones are in proximity, say 50m, the server will send a push notification, an image and some text to both iPhone.

Usage 2:
The server gets updates of the game states. When the game ends, the server tells the iPhones the game ends.

Usage3:
The server receives two images from two iPhones and then it talks to an API that runs face recogniztion



We need the server to receive some JSON and geoData from iPhones occasionally. The server then processes it and update the iPhones the game states by push notification.
The server might also need to get images from iPhones and send them to an API that does facial recognition.


 some commands so the apps on iPhones know the game state. The server also needs to
